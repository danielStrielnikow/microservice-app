spring:
  application:
   name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/danielStrielnikow/application-config/

# Eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/